MyBank Backend v4 - MySQL integration

1) Create database and tables manually:
   Execute mybank_schema.sql

2) Update credentials in src/main/resources/application.properties if needed.

3) Build & run:
   mvn clean package
   mvn spring-boot:run
